# ⚡ Быстрая настройка Supabase (5 минут)

## 🎯 Что нужно сделать:

### 1️⃣ Создать проект Supabase (2 минуты)

1. Перейдите на **https://supabase.com**
2. Нажмите **"Start your project"**
3. Войдите через GitHub
4. Нажмите **"New Project"**
5. Заполните:
   - **Name**: `fitness-trainer`
   - **Database Password**: придумайте пароль (сохраните!)
   - **Region**: выберите ближайший
   - **Plan**: **Free**
6. Нажмите **"Create new project"**
7. Подождите 1-2 минуты

---

### 2️⃣ Получить ключи (1 минута)

1. В панели Supabase → **⚙️ Settings** → **API**
2. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** ключ (длинная строка)

---

### 3️⃣ Создать таблицу (1 минута)

1. В панели → **📊 Table Editor**
2. Нажмите **"New table"**
3. **Name**: `user_registration`
4. **Enable Row Level Security**: **ВЫКЛЮЧИТЕ** (для демо)

**Добавьте колонки:**

| Column | Type | Nullable |
|--------|------|----------|
| id | int8 | ❌ (Primary Key, Auto) |
| telegram_id | text | ✅ |
| username | text | ❌ |
| password | text | ❌ |
| first_name | text | ❌ |
| last_name | text | ❌ |
| requested_role | text | ❌ |
| status | text | ❌ (Default: 'pending') |
| approved_role | text | ✅ |
| registration_date | timestamptz | ❌ (Default: now()) |
| created_at | timestamptz | ❌ (Default: now()) |
| updated_at | timestamptz | ❌ (Default: now()) |

5. Нажмите **"Save"**

---

### 4️⃣ Обновить конфигурацию (30 секунд)

1. Откройте файл `utils/supabase-config.js`
2. Замените:
   ```javascript
   const SUPABASE_URL = 'YOUR_SUPABASE_URL';
   const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
   ```
   
   На ваши значения:
   ```javascript
   const SUPABASE_URL = 'https://ваш-проект.supabase.co';
   const SUPABASE_ANON_KEY = 'ваш-ключ';
   ```

3. Сохраните

---

### 5️⃣ Загрузить на GitHub (30 секунд)

```powershell
git add .
git commit -m "Add: Подключен Supabase для хранения данных"
git push origin main
```

---

### 6️⃣ Проверить (30 секунд)

1. Откройте страницу регистрации
2. Откройте консоль (F12)
3. Должны увидеть: `✅ Используется Supabase для хранения данных`
4. Зарегистрируйтесь
5. Откройте админ-панель (можно в другом браузере!)
6. **Заявка должна быть видна!** ✅

---

## ✅ Готово!

Теперь:
- ✅ Данные на сервере Supabase
- ✅ Доступны из любого браузера
- ✅ Не удаляются при очистке кэша
- ✅ Можно просматривать в панели Supabase

---

## 🔍 Проверка в Supabase:

1. Откройте панель Supabase
2. **Table Editor** → `user_registration`
3. Должны увидеть ваши заявки!

---

## ❌ Если не работает:

1. Проверьте консоль (F12) на ошибки
2. Убедитесь, что URL и ключ правильные
3. Проверьте, что таблица создана
4. Убедитесь, что RLS отключен (или политики настроены)

---

**Подробная инструкция:** См. `SUPABASE_SETUP.md`

