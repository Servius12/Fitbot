# ✅ ЧЕКЛИСТ: Проверка всех данных

## 🔍 Быстрая проверка (2 минуты):

### 1. Откройте консоль браузера (F12)

Выполните:

```javascript
// Проверка 1: Supabase конфигурация
console.log('=== ПРОВЕРКА SUPABASE ===');
console.log('URL:', window.SUPABASE_CONFIG?.url);
console.log('Key:', window.SUPABASE_CONFIG?.anonKey ? '✅ Есть' : '❌ Нет');
console.log('Доступен:', window.SUPABASE_CONFIG?.isAvailable());

// Проверка 2: API функции
console.log('=== ПРОВЕРКА API ===');
console.log('createObject:', typeof window.trickleCreateObject);
console.log('listObjects:', typeof window.trickleListObjects);

// Проверка 3: Supabase клиент
const client = window.SUPABASE_CONFIG?.getClient();
console.log('Клиент:', client ? '✅ Инициализирован' : '❌ НЕ инициализирован');
```

**Должны увидеть:**
- URL: `https://hmxqqewurxkptxflqjca.supabase.co` ✅
- Key: `✅ Есть` ✅
- createObject: `function` ✅
- listObjects: `function` ✅
- Клиент: `✅ Инициализирован` ✅

---

## 📋 Что проверено:

### ✅ Supabase:
- **URL**: `https://hmxqqewurxkptxflqjca.supabase.co` - **УКАЗАН** ✅
- **API Key**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` - **УКАЗАН** ✅
- **Файл**: `utils/supabase-config.js` ✅

### ✅ Telegram Bot:
- **Token**: `8478471129:AAGH5iaO6TTLVM8QnSdYCSzwvtQuzlN4qdk` - **УКАЗАН** ✅
- **Файл**: `bot.py` ✅

### ⚠️ Web App URL:
- **Текущий**: `https://your-app-name.netlify.app` - **НУЖНО ОБНОВИТЬ** ⚠️
- **Файл**: `bot.py` (обновить после деплоя)

---

## 🔧 Если что-то не работает:

### Проблема: "Ошибка загрузки API"

**Решение:**
1. Откройте консоль (F12)
2. Проверьте вкладку **Network**
3. Убедитесь, что `trickle-api-supabase.js` загружен (статус 200)
4. Проверьте красные ошибки в консоли

### Проблема: "Supabase не инициализирован"

**Решение:**
1. Проверьте, что скрипт Supabase загружен:
   ```html
   <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
   ```
2. Проверьте консоль на ошибки

### Проблема: Данные не сохраняются

**Решение:**
1. Проверьте таблицу в Supabase (создана ли?)
2. Проверьте RLS (должен быть отключен)
3. Проверьте консоль на ошибки Supabase

---

## ✅ ИТОГ:

**Все данные указаны правильно!**

Если регистрация не работает:
1. Проверьте консоль (F12) на ошибки
2. Проверьте таблицу в Supabase
3. Следуйте инструкциям в `ДИАГНОСТИКА_ПРОБЛЕМ.md`

---

**Все данные на месте! Проверьте консоль!** 🔍

