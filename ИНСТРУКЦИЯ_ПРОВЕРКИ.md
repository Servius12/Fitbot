# ✅ Пошаговая проверка регистрации

## 🔍 Шаг 1: Проверка загрузки API

1. Откройте страницу регистрации: `https://ваш-домен.netlify.app/register.html`
2. Нажмите **F12** (открыть консоль разработчика)
3. Перейдите на вкладку **Console**
4. Должны увидеть:
   ```
   ✅ Trickle API: LocalStorage Demo Mode (работает без сервера)
   📦 Данные сохраняются в localStorage браузера
   ✅ localStorage доступен
   ✅ Trickle API загружен успешно
   ```

**Если видите ошибки** - проверьте путь к `utils/trickle-api.js`

---

## 🔍 Шаг 2: Попытка регистрации

1. Заполните форму регистрации:
   - Имя: `Тест`
   - Фамилия: `Тестов`
   - Логин: `test123`
   - Пароль: `123456`
   - Роль: `Клиент`
2. Нажмите **"Отправить заявку"**
3. **Смотрите в консоль** (F12) - должны увидеть:
   ```
   Отправка данных регистрации: {TelegramId: "...", Username: "test123", ...}
   Регистрация успешна! Результат: {objectId: "...", objectData: {...}}
   Проверка сохранения. Всего заявок: 1
   ```

**Если видите ошибку** - скопируйте текст ошибки из консоли

---

## 🔍 Шаг 3: Проверка сохранения данных

### Вариант A: Через консоль браузера

1. В консоли (F12) выполните:
   ```javascript
   localStorage.getItem('trickle_demo_user_registration')
   ```
2. Должны увидеть JSON с вашей заявкой

### Вариант B: Через DevTools

1. F12 → вкладка **Application** (или **Хранилище**)
2. Слева: **Local Storage** → ваш домен
3. Найдите ключ: `trickle_demo_user_registration`
4. Должны увидеть массив с объектом заявки

---

## 🔍 Шаг 4: Проверка админ-панели

1. Откройте: `https://ваш-домен.netlify.app/admin.html`
2. Войдите:
   - Логин: `admin123`
   - Пароль: `admin123`
3. Должны увидеть вашу заявку в списке!

**Если не видите:**
- Откройте консоль (F12)
- Проверьте ошибки
- Выполните в консоли:
  ```javascript
  trickleListObjects('user_registration', 100).then(r => console.log(r.items))
  ```

---

## ❌ Если что-то не работает:

### Ошибка: "trickleCreateObject не определена"

**Решение:**
1. Проверьте, что `utils/trickle-api.js` загружен
2. Проверьте путь к файлу в браузере (F12 → Network)
3. Убедитесь, что файл существует на сервере

### Ошибка: "localStorage недоступен"

**Решение:**
- Закройте приватный режим браузера
- Разрешите cookies и localStorage

### Ошибка: "Ошибка при отправке заявки"

**Решение:**
1. Откройте консоль (F12)
2. Посмотрите полный текст ошибки
3. Проверьте, что все поля заполнены
4. Попробуйте обновить страницу (Ctrl+F5)

---

## 📝 Что должно быть:

### ✅ После регистрации:
- Появляется сообщение "Заявка отправлена!"
- В консоли: "Регистрация успешна!"
- В localStorage: новая запись

### ✅ В админ-панели:
- Видна новая заявка
- Статус: "pending"
- Можно одобрить или отклонить

---

## 🆘 Нужна помощь?

1. Откройте консоль (F12)
2. Скопируйте все ошибки (красным цветом)
3. Выполните в консоли:
   ```javascript
   console.log('API доступен:', typeof trickleCreateObject !== 'undefined');
   console.log('localStorage доступен:', typeof localStorage !== 'undefined');
   const data = localStorage.getItem('trickle_demo_user_registration');
   console.log('Заявок в хранилище:', data ? JSON.parse(data).length : 0);
   ```
4. Пришлите результаты

---

**Проверьте все шаги и сообщите, на каком этапе возникает проблема!**

