<<<<<<< HEAD
# 🔧 Исправление ошибки SQL

## ❌ Ошибка:
```
ERROR: 42703: column "email" does not exist
```

## 🔍 Причина:
Таблица `user_registration` уже существует, но без колонки `email`.

---

## ✅ РЕШЕНИЕ:

### Вариант 1: Использовать миграцию (рекомендуется)

1. Откройте **SQL Editor** в Supabase
2. Скопируйте содержимое файла **`supabase-migration-add-email.sql`**
3. Вставьте и выполните
4. Готово! ✅

Этот скрипт:
- Проверит, существует ли колонка `email`
- Добавит её, если нет
- Создаст таблицу `password_reset_tokens`
- Создаст все необходимые индексы

---

### Вариант 2: Использовать исправленный скрипт

1. Откройте **SQL Editor** в Supabase
2. Скопируйте содержимое файла **`supabase-schema-fixed.sql`**
3. Вставьте и выполните
4. Готово! ✅

---

### Вариант 3: Добавить колонку вручную

Если хотите сделать вручную:

```sql
-- Добавить колонку email
ALTER TABLE user_registration ADD COLUMN email TEXT;

-- Создать индекс
CREATE INDEX IF NOT EXISTS idx_user_registration_email ON user_registration(email);
```

Затем выполните остальную часть скрипта для создания таблицы `password_reset_tokens`.

---

## 🔍 Проверка:

После выполнения скрипта проверьте:

```sql
-- Проверить структуру таблицы
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'user_registration';

-- Должна быть колонка email
```

---

## ✅ После исправления:

1. Колонка `email` будет добавлена в `user_registration`
2. Таблица `password_reset_tokens` будет создана
3. Все индексы будут созданы
4. Восстановление пароля будет работать!

---

**Используйте файл `supabase-migration-add-email.sql` - он безопасно добавит все необходимое!** 🚀

=======
# 🔧 Исправление ошибки SQL

## ❌ Ошибка:
```
ERROR: 42703: column "email" does not exist
```

## 🔍 Причина:
Таблица `user_registration` уже существует, но без колонки `email`.

---

## ✅ РЕШЕНИЕ:

### Вариант 1: Использовать миграцию (рекомендуется)

1. Откройте **SQL Editor** в Supabase
2. Скопируйте содержимое файла **`supabase-migration-add-email.sql`**
3. Вставьте и выполните
4. Готово! ✅

Этот скрипт:
- Проверит, существует ли колонка `email`
- Добавит её, если нет
- Создаст таблицу `password_reset_tokens`
- Создаст все необходимые индексы

---

### Вариант 2: Использовать исправленный скрипт

1. Откройте **SQL Editor** в Supabase
2. Скопируйте содержимое файла **`supabase-schema-fixed.sql`**
3. Вставьте и выполните
4. Готово! ✅

---

### Вариант 3: Добавить колонку вручную

Если хотите сделать вручную:

```sql
-- Добавить колонку email
ALTER TABLE user_registration ADD COLUMN email TEXT;

-- Создать индекс
CREATE INDEX IF NOT EXISTS idx_user_registration_email ON user_registration(email);
```

Затем выполните остальную часть скрипта для создания таблицы `password_reset_tokens`.

---

## 🔍 Проверка:

После выполнения скрипта проверьте:

```sql
-- Проверить структуру таблицы
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'user_registration';

-- Должна быть колонка email
```

---

## ✅ После исправления:

1. Колонка `email` будет добавлена в `user_registration`
2. Таблица `password_reset_tokens` будет создана
3. Все индексы будут созданы
4. Восстановление пароля будет работать!

---

**Используйте файл `supabase-migration-add-email.sql` - он безопасно добавит все необходимое!** 🚀

>>>>>>> 7be83a930b4950ac7ae2256d4f2ec34c8c08c5e7
