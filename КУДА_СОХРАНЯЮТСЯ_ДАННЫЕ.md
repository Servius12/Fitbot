# 📍 Куда сохраняются данные регистрации

## ✅ Краткий ответ:

**Данные сохраняются в localStorage браузера пользователя.**

---

## 📦 Детальное объяснение:

### 1. **Где физически хранятся данные:**

```
Браузер пользователя → Local Storage → Ключ: "trickle_demo_user_registration"
```

- **Местоположение**: В браузере пользователя (на его компьютере/телефоне)
- **Формат**: JSON строка в localStorage
- **Ключ**: `trickle_demo_user_registration`
- **Структура**: Массив объектов с заявками

### 2. **Как это работает:**

#### При регистрации:
1. Пользователь заполняет форму
2. Нажимает "Отправить заявку"
3. Вызывается `trickleCreateObject('user_registration', данные)`
4. Функция из `utils/trickle-api.js` сохраняет данные в localStorage
5. Данные доступны сразу в том же браузере

#### При просмотре в админ-панели:
1. Администратор открывает `admin.html`
2. Вызывается `trickleListObjects('user_registration', 100)`
3. Функция читает данные из localStorage
4. Показывает все заявки

### 3. **Структура данных:**

```json
[
  {
    "objectId": "obj_1705312345_abc123",
    "objectData": {
      "TelegramId": "123456789",
      "Username": "user123",
      "Password": "password123",
      "FirstName": "Иван",
      "LastName": "Иванов",
      "RequestedRole": "client",
      "Status": "pending",
      "RegistrationDate": "2025-01-15T10:30:00.000Z"
    },
    "createdAt": "2025-01-15T10:30:00.000Z"
  }
]
```

### 4. **Важные особенности:**

#### ✅ Преимущества:
- Работает без сервера
- Быстро и просто
- Не требует базы данных
- Подходит для демо/тестирования

#### ⚠️ Ограничения:
- Данные хранятся **только в браузере пользователя**
- Если открыть в другом браузере - данных не будет
- Если очистить кэш - данные удалятся
- Нет синхронизации между устройствами

### 5. **Как проверить сохранение:**

#### В консоли браузера (F12):
```javascript
// Посмотреть все заявки
const data = localStorage.getItem('trickle_demo_user_registration');
console.log(JSON.parse(data || '[]'));

// Или через API
trickleListObjects('user_registration', 100).then(result => {
  console.log('Всего заявок:', result.items.length);
  console.log('Заявки:', result.items);
});
```

#### В DevTools:
1. F12 → Application → Local Storage
2. Найдите `trickle_demo_user_registration`
3. Посмотрите содержимое

### 6. **Почему может не работать:**

#### ❌ Проблема 1: "Ошибка при отправке заявки"
- **Причина**: `trickle-api.js` не загружен
- **Решение**: Проверьте путь к файлу в `register.html`

#### ❌ Проблема 2: "Данные не сохраняются"
- **Причина**: localStorage заблокирован (приватный режим)
- **Решение**: Закройте приватный режим

#### ❌ Проблема 3: "Админ не видит заявки"
- **Причина**: Разные браузеры = разные localStorage
- **Решение**: Откройте админ-панель в том же браузере, где регистрировались

### 7. **Для продакшена (реальный проект):**

Для реального использования нужно:
1. **Backend сервер** (Node.js, Python, PHP)
2. **База данных** (PostgreSQL, MySQL, MongoDB)
3. **API endpoints** для сохранения/получения данных
4. **Аутентификация** и безопасность

**Текущее решение с localStorage** - только для демонстрации!

---

## 🎯 Итог:

- ✅ **Куда**: localStorage браузера
- ✅ **Ключ**: `trickle_demo_user_registration`
- ✅ **Формат**: JSON массив
- ✅ **Доступ**: Через `trickleListObjects()` и `trickleCreateObject()`
- ⚠️ **Ограничение**: Только в том же браузере

---

**Проверьте сохранение данных по инструкции в `ИНСТРУКЦИЯ_ПРОВЕРКИ.md`**

