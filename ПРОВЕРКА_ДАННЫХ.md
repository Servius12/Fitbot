# 🔍 Проверка сохранения данных

## 📍 Куда сохраняются данные регистрации:

### 1. **В localStorage браузера**
- **Ключ**: `trickle_demo_user_registration`
- **Формат**: JSON массив объектов
- **Структура каждого объекта**:
  ```json
  {
    "objectId": "obj_1234567890_abc123",
    "objectData": {
      "TelegramId": "123456789",
      "Username": "user123",
      "Password": "password123",
      "FirstName": "Иван",
      "LastName": "Иванов",
      "RequestedRole": "client",
      "Status": "pending",
      "RegistrationDate": "2025-01-15T10:30:00.000Z"
    },
    "createdAt": "2025-01-15T10:30:00.000Z"
  }
  ```

### 2. **Как проверить сохранение:**

#### В браузере (F12 → Console):
```javascript
// Проверить все заявки
const data = localStorage.getItem('trickle_demo_user_registration');
console.log(JSON.parse(data || '[]'));

// Или через API
trickleListObjects('user_registration', 100).then(result => {
  console.log('Все заявки:', result.items);
});
```

#### В браузере (F12 → Application → Local Storage):
1. Откройте DevTools (F12)
2. Вкладка "Application" (или "Хранилище")
3. Раздел "Local Storage"
4. Найдите ключ `trickle_demo_user_registration`
5. Посмотрите содержимое

### 3. **Где используются данные:**

#### Админ-панель (`admin.html`):
- **Функция**: `loadRegistrations()`
- **Вызов**: `trickleListObjects('user_registration', 100, true)`
- **Отображает**: Все заявки со статусом `pending`, `approved`, `rejected`

#### Форма входа (`login.html`):
- **Функция**: `handleSubmit()`
- **Вызов**: `trickleListObjects('user_registration', 100)`
- **Проверяет**: Логин, пароль и статус `approved`

### 4. **Проблемы и решения:**

#### ❌ Проблема: "Ошибка при отправке заявки"

**Возможные причины:**
1. `trickle-api.js` не загружен
2. localStorage недоступен (приватный режим)
3. Ошибка в коде сохранения

**Решение:**
1. Откройте консоль (F12)
2. Проверьте сообщения об ошибках
3. Проверьте, что видите: `✅ Trickle API: LocalStorage Demo Mode`
4. Если нет - проверьте путь к `utils/trickle-api.js`

#### ❌ Проблема: "Данные не сохраняются"

**Проверка:**
```javascript
// В консоли браузера
localStorage.getItem('trickle_demo_user_registration')
```

**Если null или пусто:**
- Проверьте, что `trickleCreateObject` вызывается
- Проверьте консоль на ошибки
- Убедитесь, что localStorage доступен

#### ❌ Проблема: "Админ-панель не видит заявки"

**Проверка:**
1. Откройте админ-панель
2. Откройте консоль (F12)
3. Проверьте ошибки
4. Проверьте, что данные есть в localStorage

**Решение:**
- Убедитесь, что `trickle-api.js` загружен в `admin.html`
- Проверьте, что используется тот же ключ `trickle_demo_user_registration`

### 5. **Отладка:**

#### Добавьте в консоль браузера:
```javascript
// Проверить все данные
function checkAllData() {
  const keys = Object.keys(localStorage);
  const trickleKeys = keys.filter(k => k.startsWith('trickle_demo_'));
  console.log('Все ключи Trickle:', trickleKeys);
  
  trickleKeys.forEach(key => {
    const data = JSON.parse(localStorage.getItem(key) || '[]');
    console.log(`${key}:`, data.length, 'записей');
  });
}

checkAllData();
```

### 6. **Важно:**

- ✅ Данные сохраняются **локально в браузере**
- ✅ Каждый браузер имеет свой localStorage
- ✅ Данные не синхронизируются между устройствами
- ✅ Для продакшена нужен реальный backend

---

**Для проверки:**
1. Зарегистрируйтесь
2. Откройте консоль (F12)
3. Выполните: `localStorage.getItem('trickle_demo_user_registration')`
4. Должны увидеть массив с вашей заявкой

