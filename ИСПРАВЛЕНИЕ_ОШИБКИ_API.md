# 🔧 Исправление ошибки загрузки API

## ❌ Ошибка:
```
Ошибка загрузки API. Проверьте подключение utils/trickle-api-supabase.js
```

## 🔍 Причины:

1. Скрипты загружаются асинхронно
2. Проверка происходит до полной загрузки
3. Проблемы с порядком загрузки скриптов

---

## ✅ Что исправлено:

1. ✅ Улучшена проверка загрузки API
2. ✅ Добавлены множественные попытки проверки
3. ✅ Добавлен fallback на window версии функций
4. ✅ Улучшена обработка ошибок в RegistrationForm

---

## 🔧 Что нужно сделать:

### 1. Обновить код на GitHub:

```powershell
git add .
git commit -m "Fix: Исправлена ошибка загрузки API"
git push origin main
```

### 2. Подождать обновления Netlify:

Netlify автоматически обновит сайт через 1-2 минуты.

### 3. Очистить кэш браузера:

1. Нажмите **Ctrl+Shift+R** (жесткая перезагрузка)
2. Или **Ctrl+F5**
3. Или откройте в режиме инкогнито

### 4. Проверить консоль:

1. Откройте консоль (F12)
2. Перейдите на вкладку **Console**
3. Должны увидеть:
   ```
   ✅ Supabase инициализирован
   ✅ Trickle API загружен успешно
   ✅ Trickle API: Supabase Mode (или LocalStorage Mode)
   ```

---

## 🔍 Проверка в консоли:

Выполните в консоли браузера (F12):

```javascript
// Проверить загрузку скриптов
console.log('Supabase:', typeof supabase);
console.log('SUPABASE_CONFIG:', typeof window.SUPABASE_CONFIG);
console.log('trickleCreateObject:', typeof window.trickleCreateObject);
console.log('trickleListObjects:', typeof window.trickleListObjects);
```

Должны увидеть:
- `Supabase: object` или `function`
- `SUPABASE_CONFIG: object`
- `trickleCreateObject: function`
- `trickleListObjects: function`

---

## ❌ Если все еще не работает:

### Проверка 1: Файлы загружены?

В консоли (F12) → вкладка **Network**:
- Проверьте, что `trickle-api-supabase.js` загружен (статус 200)
- Проверьте, что `supabase-config.js` загружен
- Проверьте, что `@supabase/supabase-js@2` загружен

### Проверка 2: Ошибки в консоли?

Посмотрите красные ошибки в консоли и сообщите о них.

### Проверка 3: Путь к файлам правильный?

Убедитесь, что файлы находятся в папке `utils/`:
- `utils/trickle-api-supabase.js`
- `utils/supabase-config.js`

---

## ✅ После исправления:

1. ✅ API загружается правильно
2. ✅ Функции доступны
3. ✅ Регистрация работает
4. ✅ Данные сохраняются в Supabase или localStorage

---

**Обновите код на GitHub и проверьте снова!** 🚀

